apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "categraf.fullname" . }}
  labels:
{{- include "categraf.labels" . | nindent 4 }}
data:
  config.toml: |
{{ tpl (.Files.Get "drafts/config.toml.tpl") . | indent 4 }}
